spring.application.name=FinPilot

# Database Configuration
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/finance_db}
spring.datasource.username=${DATABASE_USERNAME:postgres}
spring.datasource.password=${DATABASE_PASSWORD:rushi3717}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Spring Security OAuth2 Configuration
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:20777709312-qll2hpvjkt96ll09alg1k8tr290g2oh3.apps.googleusercontent.com}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET: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}
spring.security.oauth2.client.registration.google.scope=openid,profile,email
spring.security.oauth2.client.registration.google.redirect-uri={baseUrl}/login/oauth2/code/{registrationId}

# JWT Configuration
jwt.secret=${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

# Frontend App URL (for CORS and OAuth redirect)
app.frontend.url=${FRONTEND_URL:http://localhost:5173}

# Plaid Configuration (Sandbox Only)
plaid.client-id=${PLAID_CLIENT_ID:6aad00ae890dd9000c2d103d}
plaid.secret=${PLAID_SECRET:b4c05ad107f8a7a27eb4447de26616}
plaid.env=${PLAID_ENV:sandbox}
plaid.environment=${PLAID_ENV:sandbox}

# Ollama Local AI Configuration
ollama.base-url=${OLLAMA_BASE_URL:http://localhost:11434}
ollama.model=${OLLAMA_MODEL:qwen3:1.7b}
app.chat.ollama.keep-alive=${OLLAMA_KEEP_ALIVE:10m}